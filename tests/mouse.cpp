#include <gmock/gmock.h>

extern "C" { 
#include "mouse.h"
}

using namespace testing;

/*
TEST(mouse, mouse) {
    Mouse mouse = create_mouse();
    Maze maze = 

    move(

    ASSERT_THAT( getX(&mouse), Eq(0) );
}
*/
